# Stellas Neovim Config

Config based on [lazyvim](https://www.lazyvim.org/) nvim distro used for general purpose and (mostly)C++ programming.

## Dependecies

additional packages needed for some plugins
_arch install script_
`sudo pacman -S cmake clang composer python python-pip npm php fd luarocks lua51 wget ripgrep ast-grep markdownlint-cli2 stylua lazygit fzf imagemagick texlive-latex ghostscript kitty mermaid-cli arduino-cli arduino-language-server`

`yay -S python313`
